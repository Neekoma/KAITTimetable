package com.nicholas.timetable.lists.Timetable.ViewHolders;

import android.view.View;

import androidx.annotation.NonNull;

public class PairType1ViewHolder extends AbstractPairViewHolder implements View.OnClickListener{

    public PairType1ViewHolder(@NonNull View itemView) {
        super(itemView);
    }

    @Override
    public void onClick(View v) {
        switch (v.getId()){

        }
    }
}
